export { FaceLandmarkManager } from './FaceLandmarkManager';
export type { FaceLandmarkManagerOptions } from './FaceLandmarkManager';
// Re-export CameraManager types if helpful for the user
export { CameraManager } from 'camera-manager';
